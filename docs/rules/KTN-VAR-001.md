# KTN-VAR-001

**Sévérité**: ERROR

## Description

Les variables de package doivent être en `camelCase` (pas `SCREAMING_SNAKE_CASE`).

## Exemple conforme

```go
var (
    defaultTimeout int = 30
    maxConnections int = 100
)
```
