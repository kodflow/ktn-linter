package test008

// parseInput parse l'entrée (fonction privée)
func parseInput(input string) int {
	return len(input)
}

// formatOutput formate la sortie (fonction privée)
func formatOutput(data string) string {
	return "[" + data + "]"
}
